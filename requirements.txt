# --- Data layer (ingestion) ---
requests>=2.31
pydantic>=2.6
pyarrow>=15.0
pandas>=2.2
tqdm>=4.66

# --- HF datasets (WikiArt, Indian art styles, ArtEmis mirrors) ---
datasets>=2.18

# --- Embeddings + retrieval (CLIP + FAISS) ---
# torch is intentionally unpinned; install the build matching your platform.
# (Blackwell/RTX 50xx: install torch from the cu128/cu130 index first.)
torch>=2.2
open-clip-torch>=2.24
faiss-cpu>=1.8
Pillow>=10.2

# --- Aesthetic quality scoring (LAION predictor, museum-style curation) ---
transformers>=4.38
simple-aesthetics-predictor>=0.1.3

# --- API backend (serves the curated retriever to the React frontend) ---
fastapi>=0.110
uvicorn[standard]>=0.29
