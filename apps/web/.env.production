# Production API base — the Render URL for the Anima backend.
# Render gives you https://<service-name>.onrender.com after you create the
# service. If your service name isn't exactly "anima-api", update this and rebuild.
VITE_API_BASE=https://anima-api.onrender.com
